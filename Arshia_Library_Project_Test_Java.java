/*import java.util.List;

public class Arshia_Library_Project_Test_Java{

    public static int find_book(String book, List list_name){
        for (List shelf_row : list_name){
            if (shelf_row.contains(book)){
                inner = shelf_row.index(book);
                outer = list_name.index(shelf_row);
                return inner, outer;
            }
        }
        return -1;
    }

    public static void main(String[] args) {
        List<List<String>> triangle = List.of(
                        List.of("harry potter", "ignite me"),
                        List.of("the libray of lost things", "american royals"),
                        List.of("cinder", "stiff")
        );

        List<List<String>> circle = List.of(
                        List.of("eleven", "give and take"),
                        List.of("amoung us", "black out"),
                        List.of("one last stop", "fire with fire")
        );

        List<List<String>> square = List.of(
                        List.of("chosen", "tweet cute"),
                        List.of("infinity son", "if we were us"),
                        List.of("midnight sun", "camp")
        );

        List<List<String>> star = List.of(
                        List.of("boys dont knit", "smile"),
                        List.of("drama", "idetical"),
                        List.of("crank", "pawn")
        );

        List<List<String>> heart = List.of(
                        List.of("crossover", "everything everthing"),
                        List.of("refuge", "red queen"),
                        List.of("the book theif", "if i stay")
        );

        List<List<String>> moon = List.of(
                        List.of("heros", "tenderness"),
                        List.of("girl in peices ", "wonder"),
                        List.of("sheets", "they both die in the end")
        );

        boolean bin_triangle = true;
        boolean bin_circle = true;
        boolean bin_square = true;
        boolean bin_star = true;
        boolean bin_heart = true;
        boolean bin_moon = true;

        String path = "Book Not Found";
        float max_book_len = 24;


    

    }
}
*/

